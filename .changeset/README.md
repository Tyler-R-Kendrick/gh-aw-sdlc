# Changeset scaffolding

This directory stores release-note fragments created by the `changeset-generator` GH-AW workflow.

## Demo flow
1. Label a PR with `release-note` or `changeset`.
2. Run or wait for `changeset-generator`.
3. Review the generated markdown file before merge.
4. Keep breaking changes paired with migration notes.
